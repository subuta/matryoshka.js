import os from 'os'

export const PRAGMA = 'mat'

// add mat pragma to data.
export const START_PRAGMA = `/* ${PRAGMA} CUSTOM LOGIC [start] */`
export const END_PRAGMA = `/* ${PRAGMA} CUSTOM LOGIC [end] */`
